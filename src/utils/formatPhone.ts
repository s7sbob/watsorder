// src/utils/formatPhone.ts
export const formatPhoneNumber = (phone?: string): string => {
    if (!phone) return 'N/A';
    // Optionally, add formatting logic (e.g., add a '+' sign, spacing, etc.)
    // For now, we return the phone as-is.
    return phone;
  };
  